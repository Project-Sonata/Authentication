package com.odeyalo.sonata.authentication.entity;

/**
 * A simple enum to represent end-user's gender
 */
public enum Gender {
    MALE,
    FEMALE,
    /**
     * Used when the user prefer to not say the gender
     */
    NONE
}
